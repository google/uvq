This directory contains the UVQ 1.5 Pytorch model implementation.

To run inference with this model, please use the `uvq_inference.py` script in
the parent directory with the `--model_version 1.5` flag (or no flag, as 1.5 is default).
See `../README.md` for detailed instructions.
