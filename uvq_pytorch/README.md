This directory contains the UVQ 1.0 Pytorch model implementation.

To run inference with this model, please use the `uvq_inference.py` script in
the parent directory with the `--model_version 1.0` flag.
See `../README.md` for detailed instructions.
